{
  "buildCommand": "npm --prefix dashboard install && npm --prefix dashboard run build",
  "outputDirectory": "dashboard/dist",
  "installCommand": "npm --prefix dashboard install",
  "framework": "vite",
  "rewrites": [
    {
      "source": "/(?!api/).*",
      "destination": "/index.html"
    }
  ]
}
